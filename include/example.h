#pragma once

#include <string>

namespace cpp_template {
    /**
     * @brief Simple example function that returns a greeting message
     * @param name The name to greet
     * @return A greeting string
     */
    std::string greet(const std::string& name);
}